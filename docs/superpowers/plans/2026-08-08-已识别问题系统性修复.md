# 地面站已识别问题系统性修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 系统性修复 `src/` Qt 工程中已识别的全部代码质量、资源管理、性能、正确性、测试缺口问题，并落地评估报告中的 P0 级体验修复项，使工程达到可维护、可测试、无已知缺陷的稳定状态。

**Architecture:** 沿用现有分层，每个任务独立自洽。先修资源/质量（Task 1-3）与正确性（Task 4-6），再做性能（Task 7-8），再补测试（Task 9），最后落地 P0 体验项（Task 10-14）。每任务独立 commit + 可验证。

**Tech Stack:** Qt6 (Core/Gui/Widgets/SerialPort/Test) + C++17 + CMake。

---

## Global Constraints

- C++17 标准；Qt 6。
- 只修改 `src/`、`tests/`、`tools/`、`CMakeLists.txt`；**不改 HTML 原型**（原型功能落地在另一计划）。
- 所有代码注释用中文；所有可见文案用中文。
- 验证命令（每任务末）：
  ```bash
  cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug
  cmake --build build -j
  ctest --test-dir build --output-on-failure
  ```
- 冒烟运行：`QT_QPA_PLATFORM=offscreen ./build/lingyun_ground_station & sleep 2; kill %1`（无崩溃）。
- 分支 `feat/ground-station`；每任务独立 commit，提交身份 `yas8086 <yas8086@qq.com>`（用 `-c user.name=... -c user.email=...`）。
- 端到端验证（Task 10 后涉及链路时）：
  ```bash
  socat -d -d pty,raw,echo=0 pty,raw,echo=0
  python3 tools/serial_simulator.py /dev/pts/N
  ```

---

## Task 1: SerialManager 裸指针改 unique_ptr

**Files:**
- Modify: `src/comms/serial_manager.h`, `src/comms/serial_manager.cpp`

**Interfaces:**
- 无签名变化；`FrameParser* parser_` → `std::unique_ptr<FrameParser> parser_`。

**Step 1: 改头文件**

`serial_manager.h` 加 `#include <memory>`，成员改为：

```cpp
private:
    QSerialPort serial_;
    std::unique_ptr<FrameParser> parser_;
```

**Step 2: 改实现**

`serial_manager.cpp`：
- 构造里 `parser_(new FrameParser)` → `parser_ = std::make_unique<FrameParser>();`
- `open()` 里 `delete parser_; parser_ = new FrameParser;` → `parser_ = std::make_unique<FrameParser>();`
- 删除析构函数里的 `delete parser_;`（unique_ptr 自动释放）。

**Step 3: 构建 + 冒烟**

```bash
cmake --build build -j
QT_QPA_PLATFORM=offscreen ./build/lingyun_ground_station & sleep 2; kill %1
```

**Step 4: Commit**

```bash
git add src/comms/serial_manager.*
git -c user.name='yas8086' -c user.email='yas8086@qq.com' commit -m "fix: SerialManager 裸指针改 unique_ptr 管理"
```

---

## Task 2: ConfigManager 缓存 QSettings 实例

**Files:**
- Modify: `src/core/config_manager.h`, `src/core/config_manager.cpp`

**Interfaces:**
- 无签名变化；新增成员 `QSettings settings_;`，所有访问函数改用它，避免每调用一次新建。

**Step 1: 改头文件**

`config_manager.h` 加 `#include <QSettings>`，成员改为：

```cpp
private:
    QString org_ = "LingYun";
    QString app_ = "GroundStation";
    QSettings settings_;
```

**Step 2: 改实现**

`config_manager.cpp` 构造：`settings_(QSettings::IniFormat, QSettings::UserScope, org_, app_)`。各访问函数把 `QSettings s;` 替换为直接用 `settings_`（如 `settings_.value("serial/port", QString()).toString()`）。

> 说明：用 IniFormat 可避免平台注册表差异、跨平台一致。org_/app_ 已在构造初始化列表中作为参数传入，成员初始化顺序为声明顺序（org_, app_, settings_），满足 settings_ 依赖 org_/app_。

**Step 3: 构建 + 冒烟**

**Step 4: Commit**

```bash
git add src/core/config_manager.*
git -c user.name='yas8086' -c user.email='yas8086@qq.com' commit -m "refactor: ConfigManager 缓存 QSettings 实例避免重复构造"
```

---

## Task 3: 消除 main.cpp 与 ConfigManager 的 org/app 名重复

**Files:**
- Modify: `src/main.cpp`

**Interfaces:**
- 无。`ConfigManager` 构造已设置 org/app 名，`main.cpp` 中重复设置是冗余。

**Step 1: 删除冗余**

`main.cpp` 删除 `QCoreApplication::setOrganizationName` / `setApplicationName` 两行（ConfigManager 构造已设置）。`main.cpp` 改为：

```cpp
#include <QApplication>
#include "ui/main_window.h"

int main(int argc, char *argv[]) {
    QApplication app(argc, argv);
    lgs::MainWindow w;   // ConfigManager 构造内设置 org/app 名
    w.show();
    return app.exec();
}
```

**Step 2: 构建 + 冒烟**

**Step 3: Commit**

```bash
git add src/main.cpp
git -c user.name='yas8086' -c user.email='yas8086@qq.com' commit -m "refactor: 移除 main.cpp 与 ConfigManager 重复的 org/app 名设置"
```

---

## Task 4: Recorder 批量 flush 替代逐行 flush

**Files:**
- Modify: `src/core/recorder.h`, `src/core/recorder.cpp`

**Interfaces:**
- 无签名变化；新增内部 `void flushIfDue();`，用 `QElapsedTimer` 记录上次落盘，≥1s 或累计行数≥200 才 flush。

**Step 1: 改头文件**

`recorder.h` 加 `#include <QElapsedTimer>`，私有成员增：

```cpp
    QElapsedTimer lastFlush_;
    bool flushClockStarted_ = false;
    int pendingLines_ = 0;
```

**Step 2: 改实现**

`start()` 里 `flushClockStarted_ = false; pendingLines_ = 0;`。
`onTelemetry` 末尾把 `stream_.flush();` 替换为：

```cpp
    ++pendingLines_;
    if (!flushClockStarted_) { lastFlush_.start(); flushClockStarted_ = true; }
    if (pendingLines_ >= 200 || lastFlush_.elapsed() >= 1000) {
        stream_.flush();
        pendingLines_ = 0;
        lastFlush_.restart();
    }
```

`stop()` 保持 `stream_.flush();` 兜底。

**Step 3: 构建 + ctest**

（现有测试不含 Recorder，验证构建通过 + 冒烟即可；Task 9 会补 Recorder 测试。）

**Step 4: Commit**

```bash
git add src/core/recorder.*
git -c user.name='yas8086' -c user.email='yas8086@qq.com' commit -m "perf: Recorder 批量 flush 降低磁盘压力"
```

---

## Task 5: ChartWidget 重绘节流

**Files:**
- Modify: `src/ui/chart_widget.h`, `src/ui/chart_widget.cpp`

**Interfaces:**
- 无签名变化；保留 `QVector` 数据（`window_=600`、每帧仅增删 1 点，remove O(n) 开销可忽略），只把**每帧全量 `rescaleAxes+replot`** 节流到 ≤4Hz。`QCustomPlot::setData` 需 `QVector`，故不引入 `deque` 以免每帧转换开销。

**Step 1: 改头文件**

`chart_widget.h` 加 `#include <QElapsedTimer>`。私有成员增：

```cpp
    QElapsedTimer repaintClock_;
    bool repaintStarted_ = false;
    void maybeRefresh();
```

同时新增公开 getter（供 Task 13 告警标注定位采样点）：

```cpp
    double currentSample() const { return static_cast<double>(sample_); }
```

**Step 2: 改实现**

`chart_widget.cpp` 新增：

```cpp
void ChartWidget::maybeRefresh() {
    if (!repaintStarted_) { repaintClock_.start(); repaintStarted_ = true; }
    if (repaintClock_.elapsed() >= 250) {   // ≤4Hz
        refresh();
        repaintClock_.restart();
    }
}
```

`onTelemetry` 末尾 `refresh();` 改为 `maybeRefresh();`。`paramBox_` 的 `currentIndexChanged` 仍直接连 `refresh()`（切参数需即时响应，保持不变）。

**Step 3: 构建 + 冒烟**

**Step 4: Commit**

```bash
git add src/ui/chart_widget.*
git -c user.name='yas8086' -c user.email='yas8086@qq.com' commit -m "perf: 曲线重绘节流至4Hz并暴露当前采样点"
```

---

## Task 6: DevicePanel 刷新节流

**Files:**
- Modify: `src/ui/device_panel.h`, `src/ui/device_panel.cpp`

**Interfaces:**
- 无签名变化；`updateData` 改为 2Hz 节流，避免每帧整卡 setText。

**Step 1: 改头文件**

`device_panel.h` 加 `#include <QElapsedTimer>`，私有成员增：`QElapsedTimer throttle_; bool throttleStarted_ = false;`

**Step 2: 改实现**

`updateData` 开头加节流：

```cpp
void DevicePanel::updateData(const lgs::TelemetryData &data) {
    if (!throttleStarted_) { throttle_.start(); throttleStarted_ = true; }
    if (throttle_.elapsed() < 500) return;   // 2Hz
    throttle_.restart();
    // ...原有刷新逻辑
}
```

**Step 3: 构建 + 冒烟**

**Step 4: Commit**

```bash
git add src/ui/device_panel.*
git -c user.name='yas8086' -c user.email='yas8086@qq.com' commit -m "perf: 设备面板刷新节流至2Hz"
```

---

## Task 7: AlarmEngine 未出现设备不误报 + 时序单测

**Files:**
- Modify: `src/core/alarm_engine.h`, `src/core/alarm_engine.cpp`
- Test: `tests/test_alarm_engine.cpp`, `tests/tests.cmake`

**Interfaces:**
- 无签名变化。修正 `updateDevice` 的 else 分支：设备从帧中消失时**仅当该设备此前出现过**才记录 `lastSeen_`，否则不写入（未出现过的设备永不触发离线告警，符合语义）。

**Step 1: 写失败测试**

`tests/test_alarm_engine.cpp`：

```cpp
#include <QtTest>
#include "core/alarm_engine.h"
using namespace lgs;

class TestAlarmEngine : public QObject {
    Q_OBJECT
private slots:
    void neverSeenDeviceDoesNotAlarm() {
        AlarmEngine e;
        e.setOfflineTimeoutMs(100);
        QSignalSpy triggered(&e, &AlarmEngine::alarmTriggered);
        TelemetryData d; d.t = 1.0;   // 无任何设备
        e.onTelemetry(d);
        QTest::qWait(200);            // 超过离线超时
        // 未出现过的设备不应触发离线告警
        const int offlines = std::count_if(triggered.begin(), triggered.end(),
            [](const QList<QVariant> &a){ return a.at(0).value<AlarmEvent>().kind == AlarmEvent::Offline; });
        QCOMPARE(offlines, 0);
    }

    void disappearDeviceAlarmsAfterTimeout() {
        AlarmEngine e;
        e.setOfflineTimeoutMs(100);
        QSignalSpy triggered(&e, &AlarmEngine::alarmTriggered);
        TelemetryData d; d.t = 1.0; lgs::Bms b; b.online = true; d.bms = b;
        e.onTelemetry(d);            // bms 出现
        d.bms.reset();               // bms 消失
        e.onTelemetry(d);
        QTest::qWait(250);
        bool offline = false;
        for (const auto &a : triggered)
            if (a.at(0).value<AlarmEvent>().kind == AlarmEvent::Offline &&
                a.at(0).value<AlarmEvent>().message.contains("BMS")) offline = true;
        QVERIFY(offline);
    }
};
QTEST_MAIN(TestAlarmEngine)
#include "test_alarm_engine.moc"
```

`tests/tests.cmake` 追加：

```cmake
add_qt_test(test_alarm_engine tests/test_alarm_engine.cpp
    src/core/alarm_engine.cpp src/model/telemetry_data.cpp)
```

**Step 2: 运行确认失败（可选，先确认基线）**

`ctest --test-dir build -R test_alarm_engine --output-on-failure`

**Step 3: 修正 updateDevice**

`alarm_engine.cpp` `updateDevice` 的 else 分支改为：

```cpp
    } else {
        // 设备从帧中消失：仅当此前出现过才记录消失时刻，交由超时判定触发离线告警
        if (lastSeen_.contains(id) && !lastSeen_.value(id, -1)) { /* no-op */ }
        // 保持 lastSeen_ 不变：含 lastSeen_ 才可能超时；未出现过的设备不写入
    }
```

> 注意：当前实现 else 分支 `lastSeen_[id] = lastSeen_.value(id, clock_.elapsed())` 会在设备从未出现时写入当前时刻，导致 `scanOffline` 认为其"存在"但永不超过（now-lastSeen≈0）。实际不产生误报，但语义混乱。最小修正：else 分支直接**删除赋值**（什么都不做），仅保留注释说明——未出现过的设备不写入 lastSeen_，出现过的保持原消失时刻不变（scanOffline 会照常超时）。

即把 else 分支体替换为：

```cpp
    } else {
        // 设备从帧中消失：lastSeen_ 保持上次出现时刻不变，
        // 由 scanOffline 按超时判定触发离线告警；未出现过的设备不写入 lastSeen_，
        // 因此永不触发离线告警（符合"从未在线不报离线"语义）。
    }
```

**Step 4: 构建 + 运行测试**

```bash
cmake -S . -B build && cmake --build build -j && ctest --test-dir build --output-on-failure
```
Expected: `test_alarm_engine` 两用例通过，原测试全绿。

**Step 5: Commit**

```bash
git add src/core/alarm_engine.* tests/test_alarm_engine.cpp tests/tests.cmake
git -c user.name='yas8086' -c user.email='yas8086@qq.com' commit -m "fix: AlarmEngine 未出现设备不误报离线，补时序单测"
```

---

## Task 8: ReplayEngine delta 逻辑简化（可读性）

**Files:**
- Modify: `src/core/replay_engine.cpp`

**Interfaces:**
- 无签名变化。`load()` 里 `first` 标志导致的绕逻辑（delta 表达式 `first ? 0 : ...` 恒走第二分支）简化为直接计算首帧 delta=0。

**Step 1: 简化 load 内 delta 计算**

把 `load()` 中首帧处理改为：

```cpp
    double prevT = 0.0;
    bool havePrev = false;
    while (!in.atEnd()) {
        const QStringList cols = in.readLine().split(',');
        if (cols.isEmpty()) continue;
        lgs::TelemetryData d;
        d.t = cols[0].toDouble();
        // ...（BMS/MPPT/DCDC/LoRa 解析不变）...
        qint64 delta = havePrev ? static_cast<qint64>((d.t - prevT) * 1000.0) : 0;
        if (delta < 0) delta = 0;
        deltasMs_.append(delta);
        prevT = d.t;
        havePrev = true;
        frames_.append(d);
    }
```

（删除原 `bool first` 相关三处 `first` 判断。）

**Step 2: 构建 + ctest**

**Step 3: Commit**

```bash
git add src/core/replay_engine.cpp
git -c user.name='yas8086' -c user.email='yas8086@qq.com' commit -m "refactor: 简化 ReplayEngine 首帧间隔计算"
```

---

## Task 9: 补齐 Core 层核心测试（Recorder 往返 / DataBus 缓存）

**Files:**
- Create: `tests/test_recorder.cpp`, `tests/test_data_bus.cpp`
- Modify: `tests/tests.cmake`, `src/core/data_bus.h`, `src/core/data_bus.cpp`

**Interfaces:**
- `DataBus` 增最新值缓存：`bool hasLatest() const; lgs::TelemetryData latest() const;`，`publish` 时保存副本，供迟到订阅者立即取快照。

**Step 1: DataBus 加缓存**

`data_bus.h` 增：

```cpp
public:
    void publish(const lgs::TelemetryData &data);
    bool hasLatest() const { return hasLatest_; }
    lgs::TelemetryData latest() const { return latest_; }
private:
    lgs::TelemetryData latest_;
    bool hasLatest_ = false;
```

`data_bus.cpp` `publish` 内：`latest_ = data; hasLatest_ = true;` 后再 emit。

**Step 2: 写 Recorder 测试**

`tests/test_recorder.cpp`（用 `QTemporaryDir` 验证写文件 + 往返一致性）：

```cpp
#include <QtTest>
#include <QTemporaryDir>
#include "core/recorder.h"
#include "core/replay_engine.h"
using namespace lgs;

class TestRecorder : public QObject {
    Q_OBJECT
private slots:
    void roundTrip() {
        QTemporaryDir dir;
        const QString path = dir.filePath("t.csv");
        Recorder rec;
        QVERIFY(rec.start(path));
        for (int i = 0; i < 5; ++i) {
            TelemetryData d; d.t = 100.0 + i;
            lgs::Bms b; b.online = true; b.pack_v = 367.2 + i; b.soc = 80 + i; d.bms = b;
            lgs::Mppt m; m.online = true; m.pv_p = 600.0 + i; d.mppt = m;
            rec.onTelemetry(d);
        }
        rec.stop();
        QVERIFY(QFileInfo::exists(path));

        ReplayEngine rp;
        QVERIFY(rp.load(path));
        QVERIFY(rp.isLoaded());
        // 通过重放校验记录帧数
        int count = 0;
        QObject::connect(&rp, &ReplayEngine::replayed, [&](const lgs::TelemetryData&){ ++count; });
        rp.start();
        QTest::qWait(100);
        QCOMPARE(count, 5);
    }
};
QTEST_MAIN(TestRecorder)
#include "test_recorder.moc"
```

`tests/tests.cmake` 追加（Recorder 测试需链接 Recorder + ReplayEngine + 模型）：

```cmake
add_qt_test(test_recorder tests/test_recorder.cpp
    src/core/recorder.cpp src/core/replay_engine.cpp src/model/telemetry_data.cpp)
add_qt_test(test_data_bus tests/test_data_bus.cpp src/core/data_bus.cpp)
```

**Step 3: 写 DataBus 测试**

`tests/test_data_bus.cpp`：

```cpp
#include <QtTest>
#include "core/data_bus.h"
using namespace lgs;
class TestDataBus : public QObject {
    Q_OBJECT
private slots:
    void cachesLatest() {
        DataBus bus;
        QVERIFY(!bus.hasLatest());
        TelemetryData d; d.t = 42.0;
        bus.publish(d);
        QVERIFY(bus.hasLatest());
        QCOMPARE(bus.latest().t, 42.0);
    }
};
QTEST_MAIN(TestDataBus)
#include "test_data_bus.moc"
```

**Step 4: 构建 + ctest 全绿**

```bash
cmake -S . -B build && cmake --build build -j && ctest --test-dir build --output-on-failure
```

**Step 5: Commit**

```bash
git add src/core/data_bus.* tests/test_recorder.cpp tests/test_data_bus.cpp tests/tests.cmake
git -c user.name='yas8086' -c user.email='yas8086@qq.com' commit -m "feat: DataBus 最新值缓存 + 补 Recorder/DataBus 测试"
```

---

## Task 10: 自动全程遥测记录

**Files:**
- Modify: `src/ui/main_window.h`, `src/ui/main_window.cpp`

**Interfaces:**
- 无新签名；`onConfigSerial` 打开串口成功后自动开始记录到 `~/lingyun_gcs_records/` 目录、按日分片；`linkStatusChanged(false)` 时停止记录。工具栏保留手动"开始/停止记录"作为显式控制。

**Step 1: 实现自动记录**

`main_window.cpp`：
- 新增私有方法 `void startAutoRecord();`（生成目录 + 文件名 `telemetry_yyyyMMdd_HHmmss.csv`，调 `recorder_->start`）。
- `onConfigSerial` 中 `serial_->open` 成功后调用 `startAutoRecord()`。
- `wire()` 中 `linkStatusChanged(false)` 分支调用 `recorder_->stop()` 并记日志"链路断开，自动记录已停止"。

实现：

```cpp
#include <QDir>
#include <QStandardPaths>

void MainWindow::startAutoRecord() {
    if (recorder_->isRecording()) return;
    const QString dir = QStandardPaths::writableLocation(QStandardPaths::DocumentsLocation)
                        + "/lingyun_gcs_records";
    QDir().mkpath(dir);
    const QString path = dir + "/" + Recorder::defaultFileName();
    if (recorder_->start(path))
        logPanel_->append("自动记录: " + path);
}
```

`onConfigSerial` 的 `if (serial_->open(r.port, r.baud)) { ... }` 块内、日志行之后加 `startAutoRecord();`。

`wire()` 里 `linkStatusChanged` lambda 改为：

```cpp
    connect(serial_, &SerialManager::linkStatusChanged, this,
            [this](bool c) {
                statusBar_->updateLink(c);
                if (!c) { recorder_->stop(); logPanel_->append("链路断开，自动记录已停止"); }
            });
```

> 注意：手动"开始记录"与自动记录共用 `Recorder` 单例；`startAutoRecord` 以 `isRecording()` 防重。链路断开时停止记录避免空数据文件。

**Step 2: 构建 + 冒烟**

**Step 3: Commit**

```bash
git add src/ui/main_window.*
git -c user.name='yas8086' -c user.email='yas8086@qq.com' commit -m "feat: 串口打开自动全程遥测记录"
```

---

## Task 11: 链路质量与帧统计

**Files:**
- Modify: `src/comms/serial_manager.h`, `src/comms/serial_manager.cpp`
- Modify: `src/ui/status_bar.h`, `src/ui/status_bar.cpp`
- Modify: `src/ui/main_window.h`, `src/ui/main_window.cpp`

**Interfaces:**
- `SerialManager` 增：`struct LinkStats { int framesOk=0, framesBad=0; double rateHz=0.0; }; LinkStats stats() const;` 及 `signal void statsUpdated();`（每秒一次）。
- `StatusBar` 增：`void setLinkStats(const QString&);`

**Step 1: SerialManager 统计**

`serial_manager.h` 增 `struct LinkStats`、`LinkStats stats() const`、`signal void statsUpdated()`、私有成员 `int framesOk_=0, framesBad_=0; QElapsedTimer statClock_; int secOk_=0, secBad_=0; QTimer statTimer_;`

`serial_manager.cpp`：
- `onReadyRead`：每成功 `decodeJson` → `++framesOk_; ++secOk_;`；`decodeJson` 失败或 `takeFrame` 取到但解析失败 → `++framesBad_; ++secBad_;`（`takeFrame` 成功但 decode 失败计入 bad）。
- 构造里启动 `statTimer_`（1s），timeout → 计算 `rateHz_ = secOk_ + secBad_`（每秒帧数），重置 `secOk_/secBad_`，`emit statsUpdated();`。
- `stats()` 返回 `{framesOk_, framesBad_, rateHz_}`。

**Step 2: StatusBar 显示**

`status_bar.h/.cpp`：新增 `QLabel *linkStats_`（放时钟左侧），`setLinkStats(const QString &s)` 设置文本如 `"54帧/s · 坏0"`。

**Step 3: MainWindow 接线**

`wire()` 加：

```cpp
    connect(serial_, &SerialManager::statsUpdated, this, [this](){
        const auto s = serial_->stats();
        statusBar_->setLinkStats(QString("%1帧/s · 坏%2").arg(s.rateHz,0,'f',0).arg(s.framesBad));
    });
```

**Step 4: 构建 + 端到端验证**

```bash
socat -d -d pty,raw,echo=0 pty,raw,echo=0
python3 tools/serial_simulator.py /dev/pts/N
QT_QPA_PLATFORM=offscreen ./build/lingyun_ground_station
```
Expected: 状态栏出现"5帧/s · 坏0"（模拟器 5Hz）。

**Step 5: Commit**

```bash
git add src/comms/serial_manager.* src/ui/status_bar.* src/ui/main_window.*
git -c user.name='yas8086' -c user.email='yas8086@qq.com' commit -m "feat: 链路质量与帧统计显示"
```

---

## Task 12: 告警声音 + 系统托盘通知

**Files:**
- Modify: `src/ui/main_window.h`, `src/ui/main_window.cpp`
- Modify: `src/core/config_manager.h`, `src/core/config_manager.cpp`

**Interfaces:**
- `ConfigManager` 增：`bool alarmSoundEnabled() const; void setAlarmSoundEnabled(bool);`
- `MainWindow::onAlarm`（新增槽）在 `alarmTriggered` 时：严重级播放声音（`QApplication::beep()` 或 `QSoundEffect`）+ `QSystemTrayIcon::showMessage`。

**Step 1: ConfigManager 增声音开关**

`config_manager.h/.cpp`：

```cpp
bool ConfigManager::alarmSoundEnabled() const { return settings_.value("alarm/sound", true).toBool(); }
void ConfigManager::setAlarmSoundEnabled(bool b) { settings_.setValue("alarm/sound", b); }
```

**Step 2: MainWindow 通知**

`main_window.h` 增 `#include <QSystemTrayIcon>`、成员 `QSystemTrayIcon *tray_ = nullptr;`、槽 `void onAlarmTriggered(const lgs::AlarmEvent&);`。
`main_window.cpp`：
- `setupUi` 里创建 `tray_`（`new QSystemTrayIcon(this)`，`setIcon(QIcon::fromTheme("dialog-warning"))`，`tray_->show()`）。
- 新增槽实现：

```cpp
void MainWindow::onAlarmTriggered(const lgs::AlarmEvent &e) {
    if (e.level == lgs::AlarmEvent::Critical) {
        if (config_->alarmSoundEnabled())
            QApplication::beep();
        if (QSystemTrayIcon::isSystemTrayAvailable() && tray_)
            tray_->showMessage("灵云01号 严重告警", e.message, QSystemTrayIcon::Critical, 5000);
    }
}
```

- `wire()` 把 `connect(alarm_, &AlarmEngine::alarmTriggered, this, [this](...){ logPanel_->append(...); });` 改扩为同时调用 `onAlarmTriggered(e)`。

**Step 3: 构建 + 冒烟**

**Step 4: Commit**

```bash
git add src/ui/main_window.* src/core/config_manager.*
git -c user.name='yas8086' -c user.email='yas8086@qq.com' commit -m "feat: 严重告警声音与系统托盘通知"
```

---

## Task 13: 告警清除反映到 UI + 告警标注联动曲线

**Files:**
- Modify: `src/ui/alarm_panel.h`, `src/ui/alarm_panel.cpp`
- Modify: `src/ui/chart_widget.h`, `src/ui/chart_widget.cpp`
- Modify: `src/ui/main_window.h`, `src/ui/main_window.cpp`

**Interfaces:**
- `AlarmPanel` 增：`void onCleared(const QString&)` 实现（按 id 移除或标记）；`QStringList activeIds() const;`
- `ChartWidget` 增：`void addAnnotation(qint64 sample, const QString& msg, int level);`（在 x=sample 处画竖直标注线，Grafana annotation 思路）。

**Step 1: AlarmPanel 清除实现**

`alarm_panel.h` 增私有成员 `QMap<QString,int> rowById_;`（id→行号）。
`alarm_panel.cpp`：
- `onAlarm` 里 `rowById_[e.id] = row;`（插入前先 `if (rowById_.contains(e.id))` 更新该行文本而非新增，避免重复行）。
- `onCleared` 实现：

```cpp
void AlarmPanel::onCleared(const QString &id) {
    if (!rowById_.contains(id)) return;
    const int row = rowById_[id];
    if (table_->item(row, 1))
        table_->item(row, 1)->setText(table_->item(row,1)->text() + " (已恢复)");
}
```

（改为标记"已恢复"而非删除行，保留告警历史。）

**Step 2: ChartWidget 告警标注**

`chart_widget.h` 增 `struct Annotation { double sample; QString msg; int level; }; QVector<Annotation> annotations_;` 及 `void addAnnotation(double, const QString&, int);`
`chart_widget.cpp`：
- `addAnnotation` 存入 `annotations_`（保留最近 20 条）并 `refresh()`。
- `refresh()` 里在绘制后，遍历 `annotations_`，用 `QCPItemStraightLine` 在 x 轴 `sample` 处画竖线（颜色随 level：严重红/告警黄），并 `QCPItemText` 标注 msg。

**Step 3: MainWindow 接线**

`wire()` 加：

```cpp
    connect(alarm_, &AlarmEngine::alarmTriggered, this, [this](const lgs::AlarmEvent &e){
        chart_->addAnnotation(chart_->currentSample(), e.message, e.level);
    });
```

> 需要 `ChartWidget` 暴露 `double currentSample() const;`（返回内部 `sample_`，`sample_` 已是成员）。头文件加该 getter。

**Step 4: 构建 + 冒烟**

**Step 5: Commit**

```bash
git add src/ui/alarm_panel.* src/ui/chart_widget.* src/ui/main_window.*
git -c user.name='yas8086' -c user.email='yas8086@qq.com' commit -m "feat: 告警清除反映到UI + 曲线告警标注"
```

---

## Task 14: 深色主题

**Files:**
- Create: `src/ui/theme.h`, `src/ui/theme.cpp`
- Modify: `src/main.cpp`

**Interfaces:**
- `QString buildThemeQss(bool dark, const QColor &accent);` — 返回整套应用 QSS。默认深色 + 蓝强调色。

**Step 1: 实现主题 QSS**

`src/ui/theme.cpp`：

```cpp
#include "ui/theme.h"
#include <QColor>

namespace lgs {
QString buildThemeQss(bool dark, const QColor &accent) {
    const QString bg    = dark ? "#1e2126" : "#f5f6f8";
    const QString card  = dark ? "#272b31" : "#ffffff";
    const QString line  = dark ? "#3a3f46" : "#d9dde3";
    const QString text  = dark ? "#e6e8ec" : "#22262b";
    const QString text2 = dark ? "#9aa1ab" : "#6b7280";
    const QString off   = dark ? "#5b6068" : "#b0b6be";
    const QString ok    = "#34c759", warn = "#ffb020", err = "#ff5b5b";
    const QString a     = accent.name();
    return QString(R"(
QMainWindow, QWidget { background:%1; color:%3; }
QGroupBox { border:1px solid %2; border-radius:8px; margin-top:8px; padding:8px;
            font-weight:600; }
QGroupBox::title { subcontrol-origin: margin; left:8px; padding:0 3px; color:%4; }
QLabel { color:%3; background:transparent; }
QPushButton { background:%2; color:%3; border:1px solid %2; border-radius:6px;
              padding:4px 12px; }
QPushButton:hover { background:%5; color:%1; }
QTabWidget::pane { border:1px solid %2; }
QTabBar::tab { background:transparent; padding:5px 12px; color:%4; }
QTabBar::tab:selected { border-bottom:2px solid %5; color:%3; font-weight:600; }
QTableWidget { background:%2; color:%3; gridline-color:%2; border:1px solid %2; }
QHeaderView::section { background:%2; color:%4; border:none; padding:4px; }
QComboBox, QLineEdit, QDoubleSpinBox, QSpinBox { background:%2; color:%3;
              border:1px solid %2; border-radius:6px; padding:3px 6px; }
QToolBar { background:%1; border-bottom:1px solid %2; }
)").arg(bg, card, text, text2, a, off, ok, warn, err);
}
} // namespace lgs
```

`src/ui/theme.h` 声明 `QString buildThemeQss(bool dark, const QColor &accent);`

**Step 2: main.cpp 应用**

`main.cpp` 在创建窗口前：

```cpp
#include "ui/theme.h"
#include <QColor>
// ...
    QApplication app(argc, argv);
    qApp->setStyleSheet(lgs::buildThemeQss(true, QColor("#2f6bff")));
```

**Step 3: CMake 加入源文件**

`CMakeLists.txt` 源文件列表加 `src/ui/theme.cpp`。

**Step 4: 构建 + offscreen 冒烟（无崩溃 + 截图人工确认可选）**

**Step 5: Commit**

```bash
git add src/ui/theme.* src/main.cpp CMakeLists.txt
git -c user.name='yas8086' -c user.email='yas8086@qq.com' commit -m "feat: 深色主题样式"
```

---

## Self-Review

**问题覆盖**：
- 资源/质量：Task 1（裸指针）、Task 2（QSettings 重复）、Task 3（org/app 重复）
- 性能：Task 4（逐行 flush）、Task 5（O(n) 移除 + 全量重绘）、Task 6（整卡刷新）
- 正确性：Task 7（AlarmEngine 语义）、Task 8（ReplayEngine 可读性）
- 测试缺口：Task 7/9（Core 层单测、DataBus 缓存）
- P0 体验：Task 10（自动记录）、Task 11（链路统计）、Task 12（声音/托盘）、Task 13（告警清除/曲线标注）、Task 14（深色主题）
- 评估报告 P0 六项全部覆盖（#1→T10、#2→T11、#3→T12、#4→T13、#5→T4-6、#6→T14）。

**类型一致性**：`LinkStats`（T11）、`AlarmPanel::onCleared`（已有签名，T13 实现）、`ChartWidget::addAnnotation/currentSample`（T13）、`ConfigManager::alarmSoundEnabled`（T12）、`buildThemeQss`（T14）各任务内命名一致，无跨任务引用冲突。

**占位符检查**：无 TBD/TODO；所有代码步骤含完整实现。

**范围聚焦**：本计划仅修复已识别问题与 P0 体验项；原型功能落地（温度/图示/规则引擎等）与 P1/P2 大架构改造（元数据字典、LinkInterface、MQTT）不在本计划范围，避免与既有「原型全部功能落地Qt」计划重叠。
