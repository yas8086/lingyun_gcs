# 灵云01号 地面站对接协议（最新版）

> 面向**地面站（Qt 桌面端）开发者**的权威协议对接说明。
> 本文件依据机载端 **全部实现代码**（`airship_link` / `airship_cloud` / `airship_bms` / `airship_backup_bms` / `airship_mppt` / `airship_dcdc` / `airship_lora` / `airship_fc` / `airship_msgs` / `airship_monitor` / `airship_bringup`）逐字段核对生成，**字段、帧格式、精度规则、覆盖设备均与机载实际打包完全一致**。
>
> 地面站可通过两条链路获取机载数据：**本地串口数传**（操作/实时，主推）与 **4G MQTT 上云**（远程监控）。两链路共用同一份 JSON 打包逻辑，字段定义一致，仅传输方式与覆盖设备不同。

---

## 1. 系统概述

```
机载 (树莓派5)                                     地面
┌────────────────────────────────┐  串口数传(本地)   ┌───────────────────┐
│ BMS ──CAN                      │ ──────────────► │ 地面站 Qt 桌面端    │
│ 备用BMS ──串口(9600)           │  /dev/airship_link │ (解析 JSON 实时显示)│
│ MPPT ──CAN                     │  115200 8N1     └───────────────────┘
│ DCDC ──CAN                     │  AA 55 +JSON+\n
│ LoRa ──485(Modbus RTU)         │
│        [ link_node ] ──────────┘
│        [ cloud_node ] ────────► 4G MQTT over TLS ─► EMQX 公有云(远程)
└────────────────────────────────┘
```

- **数传链路（`link_node`）**：将 BMS / 备用BMS / MPPT / DCDC / LoRa 聚合为单行 JSON，经串口周期下传，**不含飞控 FC**、**不含告警事件**。
- **4G 链路（`cloud_node`）**：将相同设备 + **飞控 FC** 打包，经 MQTT over TLS 上报公有云（EMQX），用于远程监控。
- 两链路字段打包复用同一函数 `airship_link::pack_telemetry_json`，**字段定义完全一致**；差异仅在覆盖设备与传输方式。
- 当前阶段：**仅下传监控数据，不接收控制指令**（`safe_to_control` 机制见第 9 节，供后续控制接入）。

---

## 2. 链路参数速查

### 2.1 本地串口数传（地面站主对接链路）

| 项目 | 参数 | 配置位置 |
|------|------|---------|
| 串口设备（机载侧） | `/dev/airship_link`（udev 固定链接；代码默认 `/dev/ttyUSB0`） | `link_node.serial_device` |
| 波特率 | `115200` | `link_node.baud_rate` |
| 数据格式 | 8 数据位 / 1 停止位 / 无校验（8N1），无流控 | - |
| 传输方向 | 机载 → 地面（单向下传） | - |
| 上报频率 | 默认 **5 Hz**（200 ms） | `link_node.tx_rate_hz`（非法值≤0 回退 5.0） |
| 串口自动重连周期 | 2 s（机载侧自动，地面站无需处理） | `link_node.reconnect_ms` |

### 2.2 4G MQTT 上云（远程监控链路）

| 项目 | 参数 | 配置位置 |
|------|------|---------|
| Broker | EMQX Cloud Serverless（Europe / AWS eu-central-1） | `cloud_node.mqtt_host` |
| 端口 / 协议 | `8883` / **MQTT over TLS（必须）** | `cloud_node.mqtt_port` / `mqtt_tls_enable` |
| 遥测 Topic | `lingyun01/telemetry` | `cloud_node.mqtt_topic` |
| 认证 | 用户名 + 密码（密码经 `MQTT_PASSWORD` 环境变量注入，不落 cmdline） | `mqtt_username` / `mqtt_password` |
| TLS 证书 | 默认系统 CA `/etc/ssl/certs`（公签证书） | `mqtt_tls_ca_cert` / `mqtt_tls_insecure` |
| 上报频率 | 默认 **2 Hz**（可配） | `cloud_node.tx_rate_hz`（非法值≤0 回退 2.0） |

> 4G 仅**上行上报**，不接收任何控制指令。

---

## 3. 串口帧格式（数传链路）

每帧由**帧头 + JSON + 帧尾**构成，机载循环下传：

```
帧结构:  0xAA 0x55 <JSON> \n
```

| 段 | 内容 | 说明 |
|----|------|------|
| 帧头 | `0xAA 0x55` | 2 字节固定帧头，用于定位帧起始 |
| 数据 | `<JSON>` | 单行 JSON，**不含换行符** |
| 帧尾 | `\n` (0x0A) | 换行，标识一帧结束 |

**解析要点：**
- 地面站以 `0xAA 0x55` 定位帧头，读至 `\n` 结束一帧，中间字节即完整 JSON 文本。
- 相邻帧间无额外分隔（帧尾 `\n` 即分隔符）。
- 每帧独立完整；解析失败丢弃本帧、继续找下一帧头，不影响后续接收。
- 机载**写入失败**时主动关闭串口并自动重连（第 7 节），地面站对短暂数据中断做容错处理、可标记"链路离线"。

---

## 4. JSON 数据模型

整体为单层对象，`t` 为时间戳，其余为各设备子对象。**离线设备不出现**在帧中：

```json
{
  "t": 1785928200.123,
  "bms":    { ... },
  "backup": { ... },
  "mppt":   { ... },
  "dcdc":   { ... },
  "lora":   { "nodes": [ ... ] }
}
```

数传链路的完整顶层键（按打包顺序）：`t` → `bms` → `backup` → `mppt` → `dcdc` → `lora`。
4G 链路在此基础上，于 `dcdc` 与 `lora` 之间多出 `fc`（飞控）。

| 键 | 类型 | 说明 |
|----|------|------|
| `t` | number (double) | 机载打包时刻 Unix 时间戳 [秒]（`%.3f`，3 位小数） |
| `bms` | object | 主电源锂电池 BMS（在线时存在） |
| `backup` | object | 12S 备用电源 BMS（在线时存在） |
| `mppt` | object | MPPT 光伏控制器（在线时存在） |
| `dcdc` | object | DCDC 电源模块（在线时存在） |
| `fc` | object | 飞控状态（**仅 4G 链路**，在线时存在） |
| `lora` | object | LoRa 温度/压力采集（存在条件见下） |

> **注意 1（存在条件）**：`bms`/`backup`/`mppt`/`dcdc`/`fc` 在设备 `online=false` 或尚未收到数据时，其键**不会**出现。帧内仅剩 `{"t":...}` 表示这些设备全部离线/未就绪。
>
> **注意 2（lora 例外）**：`lora` 键只要机载收到过一轮 LoRa 采样（`samples` 非空）即出现；节点全离线时为 `"lora":{"nodes":[]}` 空数组。

---

## 5. 字段详细说明

> **类型**：`number`=JSON 数值；`bool`=`true`/`false`；`int`=十进制整数。
> **特殊值**：机载对非有限浮点（NaN/Inf）统一输出 JSON `null`，地面站须接受 `null` 并视为无效值，勿强转 0。

### 5.1 BMS（`bms`）— 主电源电池（102 串三元锂）

| 字段 | 类型 | 单位 | 说明 |
|------|------|------|------|
| `online` | bool | - | CAN 链路是否在线 |
| `pack_v` | number | V | 电池包总压 |
| `pack_i` | number | A | 电池包总电流（**充电为正**） |
| `soc` | number | % | 荷电状态 (0–100) |
| `rsoc` | number | % | 真实 SOC（`real_soc`） |
| `max_v` | number | V | 最高单体电压 |
| `min_v` | number | V | 最低单体电压 |
| `diff_v` | number | V | 单体压差 |
| `max_t` | number | ℃ | 最高单体温度 |
| `min_t` | number | ℃ | 最低单体温度 |
| `avg_t` | number | ℃ | 平均单体温度 |
| `diff_t` | number | ℃ | 单体温差 |
| `riso_p` | int | kΩ | 正极绝缘电阻 |
| `riso_n` | int | kΩ | 负极绝缘电阻 |
| `alarm` | int | - | 告警级别：`0` 正常 / `1` 故障 / `2` 严重 |

### 5.2 备用电源 BMS（`backup`）— 12S 备用电池

12S 备用电源（北辰串口协议 V1.4），经串口轮询解析。

| 字段 | 类型 | 单位 | 说明 |
|------|------|------|------|
| `online` | bool | - | 串口链路是否在线 |
| `pack_v` | number | V | 电池包总压 |
| `pack_i` | number | A | 电池包总电流（充电为正） |
| `soc` | number | % | 荷电状态 (0–100) |
| `soh` | number | % | 健康状态 (0–100) |
| `max_v` | number | V | 最高单体电压 |
| `min_v` | number | V | 最低单体电压 |
| `diff_v` | number | V | 单体压差 |
| `max_t` | number | ℃ | 最高单体温度 |
| `min_t` | number | ℃ | 最低单体温度 |
| `avg_t` | number | ℃ | 平均单体温度 |
| `diff_t` | number | ℃ | 单体温差 |
| `alarm` | int | - | 告警标志位（`alarm_word`，32 位） |
| `protect` | int | - | 保护标志位（`protect_word`，32 位） |
| `fault` | int | - | 故障标志位（`fault_word`：Bit0 放MOS失效/Bit1 充MOS失效/Bit4 短路/Bit30 保险丝/Bit31 掉线） |
| `sys` | int | - | 系统状态字（`system_word`：Bit0 放电MOS/Bit1 充电MOS/Bit5 充电器接入/Bit6 负载连接/Bit9-10 工作状态） |

> `alarm`/`protect`/`fault`/`sys` 均为 32 位无符号整数，按位与判定（`(fault & 0x1) != 0` 表示放MOS失效等）。

### 5.3 MPPT（`mppt`）

| 字段 | 类型 | 单位 | 说明 |
|------|------|------|------|
| `online` | bool | - | CAN 链路是否在线 |
| `pv_v` | number | V | 光伏电压 |
| `pv_p` | number | W | 光伏功率（计算值） |
| `batt_v` | number | V | 电池电压 |
| `charge_i` | number | A | 充电电流 |
| `today` | number | kWh | 日发电量 |
| `total` | number | kWh | 总发电量 |
| `fault` | int | - | 故障状态位（`0` 为无故障） |

### 5.4 DCDC（`dcdc`）

| 字段 | 类型 | 单位 | 说明 |
|------|------|------|------|
| `online` | bool | - | CAN 链路是否在线 |
| `in_v` | number | V | 输入电压（母线电压） |
| `out_v` | number | V | 输出电压 |
| `out_i` | number | A | 输出电流 |
| `out_p` | number | W | 输出功率（计算值） |
| `temp` | number | ℃ | 散热器温度 |
| `enabled` | bool | - | 输出是否开启 |
| `fault` | int | - | 故障状态字节（Bit 映射见厂家协议，`0` 无故障） |

### 5.5 飞控 FC（`fc`）— 仅 4G 链路

> 数传链路（`link_node`）打包时飞控传 `nullptr`，**不含 `fc` 字段**；`fc` 仅在 4G 链路（`cloud_node`）中提供。字段取自 `fc_monitor` 聚合的 `FlightStatus`，打包子集如下：

| 字段 | 类型 | 单位 | 说明 |
|------|------|------|------|
| `online` | bool | - | MAVROS 连接 + 心跳看门狗（`fc_timeout_s`=2s） |
| `roll` | number | deg | 横滚角 |
| `pitch` | number | deg | 俯仰角 |
| `yaw` | number | deg | 偏航角 |
| `lat` | number (6位) | deg | 纬度 |
| `lon` | number (6位) | deg | 经度 |
| `alt` | number | m | 相对起飞点高度（`alt_rel`） |
| `vx` | number | m/s | 东向速度 (ENU) |
| `vy` | number | m/s | 北向速度 (ENU) |
| `vz` | number | m/s | 天向速度 (ENU) |
| `mode` | string | - | 当前飞行模式（已做 JSON 转义） |
| `armed` | bool | - | 是否解锁 |
| `batt_v` | number | V | 电池电压 |
| `batt_pct` | number | - | 剩余电量（`battery_remaining`，0~1） |

> 注：`FlightStatus` 中另有 `alt_amsl`/`airspeed`/`groundspeed`/`climb_rate`/`throttle`/`motor_outputs`/`landed_state`/`roll_rate` 等字段，**当前未进入 JSON 打包**，如需展示请另行扩展打包逻辑。

### 5.6 LoRa（`lora`）— 温度/压力节点

```json
"lora": {
  "nodes": [
    { "id": 1, "online": 1, "temp": 25.4,   "pressure": 0,       "alarm": 0 },
    { "id": 2, "online": 1, "temp": 0,      "pressure": 101325,  "alarm": 0 }
  ]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `nodes` | array | 在线节点数组；**离线节点不打包**（数组内无 `online=0` 项） |
| `id` | int | 节点 ID |
| `online` | int | 恒为 `1`（仅在线节点被打包） |
| `temp` | number (℃) | 温度值；**压力节点此值为 `0`** |
| `pressure` | number (Pa) | 压力值（6 位小数）；**温度节点此值为 `0`** |
| `alarm` | int | `0` 正常 / `1` 超上限 / `-1` 超下限（仅温度节点有效） |

> 节点或为温度、或为压力传感器。可依 `temp==0`/`pressure==0` 判断类型，或按节点 ID 约定识别。空数组 `"nodes":[]` 表示本轮无在线节点。

### 5.7 可选扩展字段（未进入当前 JSON 打包）

以下字段在机载各节点已解析、存在于 `airship_msgs` 消息定义中，但**当前未打包进 JSON**。地面站如需这些数据，须先由机载在 [json_packer.cpp](../../../src/airship_link/src/json_packer.cpp) 中扩展打包逻辑（本协议随打包实现同步更新），**尚未扩展前地面站不应依赖这些字段**。

| 设备 | 未打包字段 | 说明 |
|------|-----------|------|
| `bms` | `soh` | 健康状态 [0-100 %] |
| `bms` | `pole_temps[8]` | 极耳温度 [℃] |
| `bms` | `cell_voltages[]` / `cell_temps[]` | 全部单体电压 [V] / 温度 [℃]（可变长数组，数据量较大） |
| `bms` | `fault_word1/2/3` | 一级/二级/三级故障字 |
| `backup` | `cell_voltages[]` / `cell_temps[]` | 全部单体电压 [V] / 温度 [℃] |
| `mppt` | `pv_current` | 光伏电流 [A] |
| `mppt` | `energy_month` | 月发电量 [kWh] |
| `mppt` | `air_temp` / `module_temp` | 机内空气/模块温度 [℃] |
| `mppt` | `charge_state` / `control_mode` / `charging_enabled` | 充电状态码 / 控制模式 / 充电开关 |
| `mppt` | `rated_voltage` / `rated_current` | 额定电压 / 充电额定电流 |
| `dcdc` | `ambient_temp` | 环境温度 [℃] |
| `dcdc` | `set_voltage` / `set_current` | 输出电压设定值 [V] / 输出限流 [A] |
| `fc` | `alt_amsl` | 海拔高度 [m] |
| `fc` | `roll_rate` / `pitch_rate` / `yaw_rate` | 三轴角速度 [rad/s] |
| `fc` | `airspeed` / `groundspeed` | 空速 / 地速 [m/s] |
| `fc` | `climb_rate` | 垂直爬升率 [m/s] |
| `fc` | `throttle` | 油门 [%] |
| `fc` | `motor_outputs[8]` | 电机输出（归一化 0~1，M0..M7） |
| `fc` | `landed_state` | 着陆状态（0=undefined 1=on_ground 2=in_air ...） |
| `fc` | `battery_current` | 电池电流 [A] |
| `lora` | `is_pressure` | 节点类型标志（当前由 `temp==0`/`pressure==0` 推断） |
| `lora` | `raw` | 温度寄存器原始值（qint16） |

---

## 6. 完整示例帧（数传链路）

原始字节（十六进制示意）：

```
AA 55 7B 22 74 22 3A 31 37 38 35 ... 7D 0A
│   │  └─────────── JSON ──────────┘  │
└───┘                                 └── 帧尾 \n
```

解析后的 JSON（字段齐全）：

```json
{
  "t": 1785928200.123,
  "bms": {
    "online": true, "pack_v": 367.2, "pack_i": 0.0, "soc": 85, "rsoc": 84.5,
    "max_v": 3.62, "min_v": 3.58, "diff_v": 0.04,
    "max_t": 32.5, "min_t": 31.0, "avg_t": 31.8, "diff_t": 1.5,
    "riso_p": 520, "riso_n": 498, "alarm": 0
  },
  "backup": {
    "online": true, "pack_v": 48.6, "pack_i": 1.2, "soc": 90, "soh": 96,
    "max_v": 4.18, "min_v": 4.11, "diff_v": 0.07,
    "max_t": 29.4, "min_t": 28.1, "avg_t": 28.7, "diff_t": 1.3,
    "alarm": 0, "protect": 0, "fault": 0, "sys": 3
  },
  "mppt": {
    "online": true, "pv_v": 89.5, "pv_p": 600.0, "batt_v": 86.0,
    "charge_i": 7.0, "today": 0.42, "total": 12.8, "fault": 0
  },
  "dcdc": {
    "online": true, "in_v": 86.0, "out_v": 48.1, "out_i": 5.2,
    "out_p": 250.0, "temp": 41.0, "enabled": true, "fault": 0
  },
  "lora": {
    "nodes": [
      { "id": 1, "online": 1, "temp": 25.4,   "pressure": 0,       "alarm": 0 },
      { "id": 2, "online": 1, "temp": 0,      "pressure": 101325,  "alarm": 0 }
    ]
  }
}
```

4G 链路在 `dcdc` 与 `lora` 之间插入 `fc`（见 5.5）。

---

## 7. 精度、类型与特殊值规则（重要）

地面站解析须遵循机载序列化规则，避免类型/精度误判：

| 项 | 规则 |
|----|------|
| 浮点字段（float32） | `%.4g`，**最多 4 位有效数字**（如 `367.2`、`0.0`、`600.0`），按 `double` 解析 |
| 高精度字段（double） | `lat`/`lon`/LoRa `pressure` 用 `%.6f` 固定 6 位小数（如 `31.230400`、`101325.000000`） |
| 时间戳 `t` | `%.3f`，3 位小数秒 |
| 非有限值 | NaN/Inf → JSON `null`；接受 `null` 视为无效值，勿强转 0 误导显示 |
| 字符串 | 机载已做 JSON 转义（`"`→`\"`、`\`→`\\`、换行/回车/制表→`\n`/`\r`/`\t`、其它控制符→`\uXXXX`）；标准 JSON 解析自动还原 |
| 整数/位标志 | `riso_p`/`riso_n`、`alarm`/`protect`/`fault`/`sys` 输出十进制整数，按位解析 |

---

## 8. 上报频率与时序

- **数传**：默认 200 ms（5 Hz）一帧；设备离线判定超时 `monitor_node.link_timeout_s`=3 s。
- **4G**：默认 500 ms（2 Hz）一帧。
- **离线处理**：`bms`/`backup`/`mppt`/`dcdc`（及 4G 的 `fc`）超时未上报即判离线，键从帧中消失（`lora` 除外，见第 4 节注意 2）。
- **机载串口自动重连**：检测到未打开或写入失败时按 `reconnect_ms`（2 s）周期自动重连，恢复后自动下传；地面站无需处理，数据中断期间标记"链路离线"、恢复后自动刷新。

---

## 9. 安全状态与告警（说明性）

- **安全状态**：`airship_safety` 发布 `/safety_status`（`SafetyStatus`：`safe_to_control`、`dcdc_ok`、`battery_ok`、`backup_battery_ok`、`flight_armed`、`reason`）。**当前未进入数传/4G JSON 打包**，供后续接入 DCDC/飞控控制时使用。地面站如需展示安全状态，须另行接入该消息。
- **设备告警**：`airship_monitor` 发布 `/monitor/device_alert`（`DeviceAlert`，设备类型：BMS=1/MPPT=2/DCDC=3/LoRa=4/备用BMS=5；级别 INFO=0/WARNING=1/CRITICAL=2）。**当前也未进入 JSON 打包**——数传 JSON 仅含状态快照，不含告警事件流。
- 飞控告警：`airship_fc` 发布 `/fc/alert`（`FcAlert`，LEVEL_INFO/WARNING/CRITICAL/EMERGENCY），同样未进入 JSON。
- 地面站若需**事件/告警推送**，当前需另行设计（例如由地面站对 JSON 中 `alarm`/`fault`/`online` 字段做本地阈值与状态跳变判断），或扩展机载打包逻辑。

---

## 10. 协议对接清单（地面站侧）

1. **打开串口**：按 2.1 参数（115200，8N1，无流控）。
2. **帧定位**：缓存字节流，扫描 `0xAA 0x55` 定帧头，至 `\n` 截取 JSON。
3. **JSON 解析**：解析对象；`t` 为时间戳，其余键分发到 BMS/备用BMS/MPPT/DCDC/LoRa 结构。
4. **容错**：
   - JSON 解析失败 → 丢弃本帧；
   - 设备键不存在 → 视为离线；
   - 字段值 `null`（NaN/Inf）→ 按无效值处理；
   - 位标志按位与判定。
5. **显示**：按 `online` 与各字段刷新界面；`alarm`/`fault` 非 0 提示告警。
6. **LoRa 类型识别**：`temp==0`/`pressure==0` 区分温度/压力节点。
7. **（可选）4G 远程**：订阅 `lingyun01/telemetry` 获取含 `fc` 的完整数据，TLS 8883。

---

## 11. 与 4G 远程链路的区别（范围说明）

| 维度 | 数传（本地 `link_node`） | 4G（远程 `cloud_node`） |
|------|--------------------|-------------------|
| 范围 | 本地（地面站旁） | 异地/公网 |
| 协议 | 串口 + 自定义帧（AA 55 JSON \n） | MQTT over TLS（8883） |
| 频率 | 5 Hz | 2 Hz（可配） |
| 方向 | 机载 → 地面 | 机载 → 公有云 |
| 覆盖设备 | BMS / 备用BMS / MPPT / DCDC / LoRa | BMS / 备用BMS / MPPT / DCDC / LoRa / **飞控 FC** |
| 飞控 FC | **不含**（打包传 `nullptr`） | **含**（`fc` 字段） |
| 告警/安全 | **均不含**（仅状态快照） | **均不含**（仅状态快照） |

> 两链路字段打包复用同一 `pack_telemetry_json`，字段定义一致；差异仅在覆盖设备（`fc`）与传输方式。若需本地地面站显示飞控数据，应在 `link_node` 增加 `/fc/status` 订阅并传入 `&fc_`，使两链路一致。

---

## 12. 联调与测试

- **机载模拟**：运行 `tools/` 下模拟器（`vcan0` 模拟 CAN、`sim_*` 脚本），启动 `link_node` 观察串口输出，验证地面站解析。
- **机载实机**：启动 `device_monitor.launch.py` 后串口按帧下传（systemd 部署见 [01_设备监控架构](../../01_设备监控架构.md)）。
- **验证工具**：`minicom`/`cat` 查看串口原始数据；用任意 JSON 工具校验帧内 JSON 合法性。
- **4G 验证**：`mosquitto_sub -h <broker> -p 8883 --cafile <ca> -u <user> -P <pwd> -t lingyun01/telemetry` 订阅核对。

---

## 附：消息定义与打包实现参考

- 打包实现：[json_packer.cpp](../../../src/airship_link/src/json_packer.cpp)
- 数传节点：[link_node.cpp](../../../src/airship_link/src/link_node.cpp)
- 4G 节点：[cloud_node.cpp](../../../src/airship_cloud/src/cloud_node.cpp)、[mqtt_client.cpp](../../../src/airship_cloud/src/mqtt_client.cpp)
- 消息定义：`airship_msgs/msg/`（BmsStatus / BackupBmsStatus / MpptStatus / DcdcStatus / FlightStatus / LoRaSamples / LoRaSample / LoRaSummary / DeviceAlert / FcAlert / SafetyStatus）
- 参数配置：[airship_params.yaml](../../../src/airship_bringup/config/airship_params.yaml)
- 启动编排：[device_monitor.launch.py](../../../src/airship_bringup/launch/device_monitor.launch.py)
